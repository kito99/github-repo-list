{
  "name": "virtua-github-repo-list",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "es-dev-server --app-index /src/index.html --node-resolve --watch --open --base-path src",
    "start:compatibility": "es-dev-server --compatibility all --app-index /src/index.html --node-resolve --watch --open --base-path src",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Kito D. Mann (virtua.tech)",
  "license": "MIT",
  "dependencies": {
    "lit-element": "^2.4.0"
  },
  "devDependencies": {
    "es-dev-server": "^1.57.5",
    "typescript": "^4.0.3"
  }
}
