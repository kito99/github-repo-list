{
  "name": "virtua-github-repo-list",
  "version": "2.0.0",
  "description": "Example application showing use of Lit and Web Components.",
  "scripts": {
    "start": "wds --app-index /src/index.html --node-resolve --watch --open --base-path /src/index.html",
    "start:compatibility": "wds --compatibility all --app-index /src/index.html --node-resolve --watch --open --base-path src",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Kito D. Mann (virtua.tech)",
  "license": "MIT",
  "dependencies": {
    "lit": "^3.2.1"
  },
  "devDependencies": {
    "@web/dev-server": "^0.4.6",
    "typescript": "^5.8.2"
  }
}
